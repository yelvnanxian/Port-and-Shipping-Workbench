# 船司适配器

船司官网查询统一由 `server/automation` 下的真实 TrackingProvider 负责。旧版占位适配器已移除；新增解析器应接入 provider 路由，并只写入官方响应中实际解析出的时间。

接入真实数据时建议优先使用船司官方 API；如需抓取公开网页，应遵循对方 robots.txt、服务条款和访问频率限制。登录凭证放在环境变量或密钥服务中，不要提交到仓库。

一个适配器只处理单一数据源。页面、筛选和 Excel 导出不需要随船司变化。
